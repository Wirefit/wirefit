# wirefit

**Language-agnostic contract checking.**

This crate is a **placeholder** reserving the `wirefit` name on crates.io. The tool
currently ships as a Go binary and GitHub Action. A Rust extractor (`schemars`, via the
public extractor protocol) is planned.

- Project & docs: https://github.com/wirefit/wirefit
- License: Apache-2.0
